/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "9btwh0s1u8zcjmc0.public.blob.vercel-storage.com",
      },
    ],
  },
};

module.exports = nextConfig;
